apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Values.image.name }}-post-delete-job
  labels:
    release: {{ .Release.Name }}
    chart: {{ .Chart.Name }}
    version: {{ .Chart.Version }}
  annotations:
    # 注意，如果没有下面的这个注释的话，当前的这个Job就会被当成release的一部分
    "helm.sh/hook": post-delete
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  template:
    metadata:
      name: {{ .Values.image.name }}-post-delete
      labels:
        release: {{ .Release.Name }}
        chart: {{ .Chart.Name }}
        version: {{ .Chart.Version }}
    spec:
      restartPolicy: Never
      containers:
        - name: post-delete-job
          image: "{{ include "mergedGlobalValues.image" . }}"
          command:
            - "sh"
            - "-c"
            - >
              curl -X DELETE http://studio-web-service:8877/api/workstation/v1/webapp/mf-model-manager
              
