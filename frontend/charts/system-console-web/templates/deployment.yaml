{{- if semverCompare ">=1.16-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: apps/v1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Deployment
metadata:
  name: {{ include "template.fullname" . }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "template.labels" . | nindent 4 }}
spec:
  {{- with .Values.stratege }}
  strategy:
  {{- toYaml . | nindent 4 }}
  {{- end }}
  {{ if not .Values.maxReplicaCount}}
  replicas: {{ .Values.replicaCount }}
  {{- else }}
  replicas: {{ min .Values.replicaCount .Values.maxReplicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "template.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/rules: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "template.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.dnsConfig }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "template.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      volumes:
      - name: depservice
        configMap:
          name: {{ include "template.fullname" $}}
      - name: host-time
        hostPath:
          path: /etc/localtime
      containers:
        - name: deploy-web-service
          securityContext:
            {{- toYaml $.Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.registry }}/{{ .Values.image.deploy_web_service.repository }}:{{ .Values.image.deploy_web_service.tag }}"
          imagePullPolicy: {{ $.Values.image.pullPolicy }}
          livenessProbe:
            failureThreshold: 5
            httpGet:
              path: /health/alive
              port: 18080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            failureThreshold: 5
            httpGet:
              path: /health/ready
              port: 18080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          startupProbe:
            exec:
              command:
              - echo
              - no startupProbe
            failureThreshold: 30
            periodSeconds: 10
          command: 
          - "node"
          args:
            - /AnyShare/DeployWebService/build/bundle.js
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
          - name: depservice
            mountPath: "/etc/globalConfig/depservice"
          env:
          - name: TZ
            value: "{{ $.Values.env.timezone }}"
          - name: LANG
            value: {{ $.Values.service.language | default "zh_CN" }}
          {{- range $k, $v := $.Values.env }}
          - name: {{ $k | quote }}
            value: {{ $v | quote }}
          {{- end }}
        - name: deploy-web-static
          securityContext:
            {{- toYaml $.Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.registry }}/{{ .Values.image.deploy_web_static.repository }}:{{ .Values.image.deploy_web_static.tag }}"
          imagePullPolicy: {{ $.Values.image.pullPolicy }}
          livenessProbe:
            failureThreshold: 5
            httpGet:
              path: /health/alive
              port: 18800
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            failureThreshold: 5
            httpGet:
              path: /health/ready
              port: 18800
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          startupProbe:
            exec:
              command:
              - echo
              - no startupProbe
            failureThreshold: 30
            periodSeconds: 10
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
          - name: depservice
            mountPath: "/etc/globalConfig/depservice"
          env:
          - name: TZ
            value: "{{ $.Values.env.timezone }}"
          - name: LANG
            value: {{ $.Values.service.language | default "zh_CN" }}
          {{- range $k, $v := $.Values.env }}
          - name: {{ $k | quote }}
            value: {{ $v | quote }}
          {{- end }}
        - name: business-system-frontend
          image: "{{ .Values.image.registry }}{{ .Values.image.business_system.repository }}:{{ .Values.image.business_system.tag }}"
          imagePullPolicy: {{ $.Values.image.pullPolicy }}
          ports:
            - name: businesshttp
              containerPort: 88
            - name: businesshttps
              containerPort: 440
          volumeMounts:
            - name: host-time
              mountPath: /etc/localtime
          resources:
            requests:
              cpu: 150m
              memory: 250Mi
            limits:
              cpu: 2
              memory: 2Gi
          livenessProbe:
            tcpSocket:
              port: 88
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 88
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          startupProbe:
            tcpSocket:
              port: 88
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
        - name: mf-model-manager-nginx
          image: "{{ .Values.image.registry }}{{ .Values.image.model_manager.repository }}:{{ .Values.image.model_manager.tag }}"
          imagePullPolicy: {{ $.Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
            - name: https
              containerPort: 443
          volumeMounts:
            - name: host-time
              mountPath: /etc/localtime
          resources:
            requests:
              cpu: 150m
              memory: 250Mi
            limits:
              cpu: 2
              memory: 2Gi
          livenessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          startupProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
        {{- with $.Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
        {{- end }}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 1
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app.kubernetes.io/instance
                  operator: In
                  values:
                  - {{ .Release.Name }}
              topologyKey: kubernetes.io/hostname
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
