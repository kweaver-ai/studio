{{- $depServices := include "mergedGlobalValues.depServices" . | fromYaml -}}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "template.fullname" . }}
  namespace: {{ .Release.Namespace }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ include "mergedGlobalValues.ingressClassName" . }}
  rules:
  - http:
      paths:
      - backend:
          service:
            name: {{ include "template.fullname" $ }}-service
            port:
              number: 18080
        path: /interface/deployweb
        pathType: Prefix
      - backend:
          service:
            name: {{ include "template.fullname" $ }}-service
            port:
              number: 18080
        path: /api/deploy-web-service
        pathType: Prefix
      - backend:
          service:
            name: {{ include "template.fullname" $ }}-service
            port:
              number: 18080
        path: /api/deployweb
        pathType: Prefix
      - backend:
          service:
            name: {{ include "template.fullname" $ }}-static
            port:
              number: 18800
        path: /deploy
        pathType: Prefix
{{- end }}

---
{{- if semverCompare "<1.19-0" .Capabilities.KubeVersion.GitVersion }}
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ include "template.fullname" . }}
  namespace: {{ .Release.Namespace }}
  annotations:
    kubernetes.io/ingress.class: {{ include "mergedGlobalValues.ingressClassName" . }}
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  rules:
  - http:
      paths:
      - backend:
          serviceName: {{ include "template.fullname" $ }}-service
          servicePort: 18080
        path: /interface/deployweb
      - backend:
          serviceName: {{ include "template.fullname" $ }}-service
          servicePort: 18080
        path: /api/deployweb
      - backend:
          serviceName: {{ include "template.fullname" $ }}-service
          servicePort: 18080
        path: /api/deploy-web-service
      - backend:
          serviceName: {{ include "template.fullname" $ }}-static
          servicePort: 18800
        path: /deploy
      
{{- end }}
